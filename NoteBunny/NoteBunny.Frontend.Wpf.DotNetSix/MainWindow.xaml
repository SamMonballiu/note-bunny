<Window x:Class="NoteBunny.Frontend.Wpf.DotNetSix.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NoteBunny.Frontend.Wpf.DotNetSix" xmlns:viewmodels="clr-namespace:NoteBunny.FrontEnd.Wpf.DotNetSix.Viewmodels" d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" 
        SizeToContent="Height" MinHeight="500" Width="1000" MinWidth="900" Background="#eee"
        ResizeMode="CanMinimize"
        >
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem x:Name="menu_Tags" Header="_Tags" Click="Menu_Tags_Click"/>
            </MenuItem>
        </Menu>
        <StackPanel DockPanel.Dock="Top" >

            <Grid Margin="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.8*"/>
                    <ColumnDefinition Width="0"/>
                    <ColumnDefinition Width="1.4*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="10">
                    <TextBox 
                        MinWidth="{Binding ElementName=lstNotes, Path=ActualWidth}" 
                        Margin="0 0 0 12" DockPanel.Dock="Left" 
                        Name="txtSearchAlt" 
                        Height="24" 
                        KeyDown="TxtSearchAlt_KeyDown"
                        Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"/>
                    <ListBox 
                        x:Name="lstNotes" 
                        ItemsSource="{Binding NoteModels}" 
                        FontSize="12" 
                        MinHeight="360" 
                        MaxHeight="360" 
                        SelectionChanged="LstNotes_SelectionChanged"
                        DisplayMemberPath="Subject"
                        SelectedValuePath="Id"
                    />

                    <Grid Margin="0 8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>

                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <ComboBox Grid.Column="0" x:Name="cbxSortOptions" Margin="0 0 4 0" SelectedIndex="0" SelectionChanged="CbxSortOptions_SelectionChanged"/>
                        <ComboBox Grid.Column="1" x:Name="cbxSortDirection" Margin="4 0 0 0" SelectedIndex="0" />
                    </Grid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Grid Visibility="Visible">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" x:Name="btn_NewNote" Padding="4" Margin="0 0 4 0" Click="Btn_NewNote_Click">_New</Button>
                        </Grid>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="2" x:Name="spSelected" Margin="10" Visibility="Hidden">
                    <Viewbox Height="48" HorizontalAlignment="Left">
                        <TextBlock x:Name="lblSubject" TextWrapping="WrapWithOverflow" Text="{Binding Subject}" Style="{StaticResource SubjectHeader}"/>
                    </Viewbox>
                    <Label x:Name="txtDetails" Content="{Binding Details}" Style="{StaticResource DetailLine}" Height="32"/>
                    <StackPanel x:Name="txtOnView">
                        <DockPanel Margin="8 0 22 0">
                            <TextBlock VerticalAlignment="Center"  Width="20" Text="{Binding ElementName=txtContent, Path=FontSize, FallbackValue=--}"/>
                            <Slider Maximum="64" Minimum="12" TickFrequency="2" IsSnapToTickEnabled="True" TickPlacement="TopLeft" Value="{Binding ElementName=txtContent, Path=FontSize}" />
                        </DockPanel>
                        <ScrollViewer MaxHeight="360" MinHeight="200" Margin="0 12 0 0">
                            <StackPanel>
                                <Border BorderBrush="DarkGray" BorderThickness="1">
                                    <TextBlock 
                                        x:Name="txtContent" 
                                        MinHeight="200"
                                        Text="{Binding Content}" 
                                        Style="{StaticResource NoteContent}" 
                                        TextWrapping="WrapWithOverflow" 
                                        FontSize="12" 
                                        Padding="12"
                                        />
                                </Border>
                                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Margin="0 8 0 0">
                                    <Button x:Name="btn_EditNote" Padding="4" Margin="4 0 4 0" Click="Btn_EditNote_Click">_Edit</Button>
                                    <Button x:Name="btn_DeleteNote" Padding="4" Margin="4 0 0 0" Command="{Binding RelativeSource={RelativeSource AncestorType=Window, Mode=FindAncestor}, Path=DataContext.OnDeleteSelectedNote}">Delete</Button>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>
                    </StackPanel>
                </StackPanel>
            </Grid>

        </StackPanel>
        <StatusBar DockPanel.Dock="Top" x:Name="status" Height="24" Background="#ddd">
            <TextBlock x:Name="statusTxt" TextAlignment="Right" HorizontalAlignment="Right"/>
        </StatusBar>
    </DockPanel>
</Window>
